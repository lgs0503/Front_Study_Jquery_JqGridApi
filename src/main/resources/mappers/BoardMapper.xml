<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bbs.mapper.BoardMapper">

	<resultMap type="com.bbs.vo.BoardVO" id="boardMap">
		<id property="bno" column="bno"></id>
		<result property="title" column="title"/>
		<result property="content" column="content"/>
		<result property="writer" column="writer"/>
		<result property="regDate" column="regDate"/>
	</resultMap>
	
	<sql id="boardWhere">
		<if test="searchKey != null and searchKey != '' and searchKey == 'title' and keyword !='' and keyword != null ">
		AND		TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchKey != null and searchKey != '' and searchKey == 'writer' and keyword !='' and keyword != null ">
		AND		WRITER LIKE '%' || #{keyword} || '%'
		</if>
	</sql>
	
	<select id="getBoardList" resultMap="boardMap" parameterType="BoardGridSearchVO">
		SELECT 	BNO,
				TITLE,
				CONTENT,
				WRITER,
				REGDATE
		FROM	(
				SELECT	ROW_NUMBER() OVER(ORDER BY BNO) AS NUM,
						BNO,
						TITLE,
						CONTENT,
						WRITER,
						TO_CHAR(REGDATE, 'YYYY-MM-DD') AS REGDATE
				FROM	BOARD
				WHERE 	1=1
				<include refid="boardWhere"></include>
				) A
		WHERE	NUM BETWEEN #{startRow} AND #{endRow}
		ORDER BY ${sidx} ${sord}
	</select>

	<select id="getBoardCount" resultType="int" parameterType="BoardGridSearchVO">
		SELECT	CEIL(COUNT(*) / #{rows}) CNT
		FROM	BOARD
		WHERE 	1=1
		<include refid="boardWhere"></include>
	</select>
	
	<insert id="insertBoard" parameterType="BoardVO">
		INSERT
		INTO	BOARD
				(
				BNO,
				TITLE,
				CONTENT,
				WRITER,
				REGDATE
				)
		VALUES	(
				(
				SELECT	MAX(NVL(BNO,0))+1 
				FROM	BOARD
				),
				#{title},
				#{content},
				#{writer},
				#{regDate}
				)
	</insert>
	
	<update id="updateBoard" parameterType="BoardVO">
		UPDATE	BOARD
		SET		TITLE		= #{title},
				CONTENT		= #{content},
				WRITER		= #{writer},
				REGDATE		= #{regDate}
		WHERE	BNO			= #{bno}
	</update>
	
	<delete id="deleteBoard" parameterType="BoardVO">
		DELETE 
		FROM	BOARD
		WHERE 	BNO = #{bno}
	</delete>
</mapper>